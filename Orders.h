#pragma once
#include "Item.h"
//TODO there are many posibilities here depends on the time and how much code is actually required
//But base case is simple what ordered through the fridge and probably wait day or two until its added to the fridge
//For that i will need to make a system that will keep the "current" date because for now its just a constant
//this changing date will probably be included in main as driver code.